<div class="navbar navbar-expand-sm navbar-dark bg-dark navbar-lewagon">
  <a class="navbar-brand mx-4" href="/">
    <%= image_tag "logo_yoohood_alta.png", alt: 'yoohood logotype' %>
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link" href="#">my events</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">all events</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">favorites</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">new event</a>
        </li>


        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <% placeholder = "https://www.wittenberg.edu/sites/default/files/2018-10/nouser_0.jpg" %>
            <%#= image_tag current_user.photo.url ? current_user.photo.url : placeholder , class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
            <%= image_tag placeholder , class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <%= link_to root_path, class: 'dropdown-item' do %>
                <div class="d-flex justify-content-between align-items-center">
                  invitations
                  <i class="fas fa-ticket-alt"></i>
                </div>
              <% end %>
              <div class="dropdown-divider"></div>
              <%= link_to root_path, class: 'dropdown-item' do %>
                <div class="d-flex justify-content-between align-items-center">
                  settings
                  <i class="fas fa-cog"></i>
                </div>
              <% end %>
              <div class="dropdown-divider"></div>
              <%= link_to root_path, class: 'dropdown-item' do %>
                <div class="d-flex justify-content-between align-items-center">
                  rewards
                  <i class="fas fa-trophy"></i>
                </div>
              <% end %>
              <div class="dropdown-divider"></div>
              <%= link_to destroy_user_session_path, class: 'dropdown-item', method: :delete do %>
                <div class="d-flex justify-content-between align-items-center">
                  log out
                  <i class="fas fa-sign-out-alt"></i>
                </div>
              <% end %>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "members", members_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
</div>
