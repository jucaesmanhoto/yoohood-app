<div class="navbar navbar-expand-sm navbar-dark bg-dark navbar-lewagon">
  <a class="navbar-brand mx-4" href="/">
    <%= image_tag "logo_yoohood_alta.png", alt: 'yoohood logotype' %>
  </a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <% if user_signed_in? %>
          <li class="nav-item d-flex justify-content-end">
            <%= simple_form_for 'coordinates', url: nearby_path do |f| %>
              <%= f.hidden_field :latitude, id: 'latitude-input' %>
              <%= f.hidden_field :longitude, id: 'longitude-input'%>
              <%= f.submit t('.nearby'), class: "nav-link d-none text-right", id: 'nearby-button' %>
            <% end %>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/my_events"><%= t('.my_events') %></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/events"><%= t('.all_events') %></a>
          </li>
        <% end %>

        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <% placeholder = current_user.facebook_picture_url ? current_user.facebook_picture_url : "https://www.wittenberg.edu/sites/default/files/2018-10/nouser_0.jpg" %>
            <%= image_tag current_user.photo.url ? current_user.photo.url : placeholder , class: "avatar dropdown-toggle ml-3", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false %>
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">

              <!-- items to implement -->
              <!-- <%= link_to root_path, class: 'dropdown-item' do %>
                <div class="d-flex justify-content-between align-items-center">
                  <%= t('.invitations') %>
                  <i class="fas fa-ticket-alt"></i>
                </div>
              <% end %>
              <div class="dropdown-divider"></div>
              <%= link_to root_path, class: 'dropdown-item' do %>
                <div class="d-flex justify-content-between align-items-center">
                  <%= t('.rewards') %>
                  <i class="fas fa-trophy"></i>
                </div>
              <% end %>
              <div class="dropdown-divider"></div>
              <%= link_to root_path, class: 'dropdown-item' do %>
                <div class="d-flex justify-content-between align-items-center">
                  <%= t('.settings') %>
                  <i class="fas fa-cog"></i>
                </div>
              <% end %> 
              <div class="dropdown-divider"></div>-->
              <%= link_to profile_path, class: 'dropdown-item' do %>
                <div class="d-flex justify-content-between align-items-center">
                  <%= t('.my_profile') %>
                  <i class="fas fa-user"></i>
                </div>
              <% end %>
              <div class="dropdown-divider"></div>
              <%= link_to destroy_user_session_path, class: 'dropdown-item', method: :delete do %>
                <div class="d-flex justify-content-between align-items-center">
                  <%= t('.log_out') %>
                  <i class="fas fa-sign-out-alt"></i>
                </div>
              <% end %>
            </div>
          </li>
        <% elsif @page_name != 'members' %>
          <li class="nav-item">
            <%= link_to t('.members'), members_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
</div>
